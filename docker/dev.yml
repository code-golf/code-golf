services:
  app:
    ports:
      - 80:80
      - 443:443
  assets:
    build:
      context: ..
      dockerfile: docker/assets.Dockerfile
    command:
      - ./esbuild
      - --watch=forever
    stop_signal: SIGKILL
    user: 1000:1000
  db:
    ports:
      - 5432:5432
