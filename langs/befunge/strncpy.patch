--- /lib/genx/genx.c
+++ /lib/genx/genx.c
@@ -219,7 +219,7 @@
 		if ((w->status = growCollector(c, sl)) != GENX_SUCCESS)
 			return GENX_ALLOC_FAILED;
 
-	strlcpy((char *) c->buf, (const char *) string, c->space);
+	strncpy((char *) c->buf, (const char *) string, c->space);
 	return GENX_SUCCESS;
 }
 
